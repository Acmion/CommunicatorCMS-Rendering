@using CommunicatorCms.Core.Settings;
@using CommunicatorCms.Core.AppFileSystem;

@{
    /*
    var activeThemeGetActionFilePath = AppPath.Join(GeneralSettings.WebThemesPath, ThemeSettings.ActiveName, RenderingSettings.GetActionFileName);
    var defaultThemeGetActionFilePath = AppPath.Join(GeneralSettings.WebThemesPath, ThemeSettings.DefaultName, RenderingSettings.GetActionFileName);

    if (AppFile.Exists(activeThemeGetActionFilePath))
    {
        await Html.RenderPartialAsync(activeThemeGetActionFilePath);
    }
    else if (AppFile.Exists(defaultThemeGetActionFilePath))
    {
        await Html.RenderPartialAsync(defaultThemeGetActionFilePath);
    }
    */
}